# Flask settings
FLASK_ENV=development
LOG_LEVEL=DEBUG  # Override to DEBUG for more verbose logging
FLASK_DEBUG=1
FLASK_APP=app.py
# Host and port for local dev
FLASK_RUN_HOST=0.0.0.0
FLASK_RUN_PORT=5000

# Database (if using SQLite, this can be ignored or set to a dev DB)
DATABASE_URL=sqlite:///data/dev.db

# Other environment variables
SECRET_KEY=dev-secret-key-change-in-production
# Example: path to Excel data (if needed)
DATA_PATH=./data

# Any other dev-specific settings
